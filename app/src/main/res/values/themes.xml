<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Project" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Project" parent="Base.Theme.Project" />
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <!-- Set the splash screen background color -->
        <item name="windowSplashScreenBackground">@color/CrimsonRed</item>

        <!-- Use your app logo/icon -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ow_c_logo</item>

        <!-- Optional animation duration -->
        <item name="windowSplashScreenAnimationDuration">200</item>

        <!-- Set the theme of the Activity that follows your splash screen -->
        <item name="postSplashScreenTheme">@style/Base.Theme.Project</item>
    </style>
</resources>